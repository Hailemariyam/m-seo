╔═══════════════════════════════════════════════════════════════════════════╗
║                    M-SEO REACT - QUICK REFERENCE                          ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌───────────────────────────────────────────────────────────────────────────┐
│ 🚀 START TESTING NOW                                                      │
└───────────────────────────────────────────────────────────────────────────┘

  The test server is ALREADY RUNNING!

  Open: http://localhost:3000

  Or run manually:
  $ npm run test:react

  Alternative methods:
  $ npx serve test-app -p 3000           # Node.js (recommended)
  $ npx http-server test-app -p 3000     # Node.js alternative
  $ python3 -m http.server 3000 --directory test-app  # Python fallback


┌───────────────────────────────────────────────────────────────────────────┐
│ 📖 BASIC USAGE                                                            │
└───────────────────────────────────────────────────────────────────────────┘

  import { useSeo } from 'm-seo/adapters/ReactSPAAdapter';

  function MyPage() {
    useSeo({
      title: 'My Page',
      description: 'My description',
      keywords: ['react', 'seo'],
      canonical: 'https://example.com',
      ogImage: 'https://example.com/image.jpg'
    });

    return <div>Content</div>;
  }


┌───────────────────────────────────────────────────────────────────────────┐
│ 🔍 VERIFY IT'S WORKING                                                    │
└───────────────────────────────────────────────────────────────────────────┘

  Method 1: Live Inspector (built into test app)
    → Scroll down on any test page
    → See "🔍 Live SEO Inspector" section

  Method 2: DevTools
    → Press F12
    → Elements tab → <head>
    → Find: <meta data-mseo="true">

  Method 3: Console
    → F12 → Console
    → Run: document.querySelectorAll('meta[data-mseo]')


┌───────────────────────────────────────────────────────────────────────────┐
│ 📁 KEY FILES                                                              │
└───────────────────────────────────────────────────────────────────────────┘

  test-app/README.md ────────→ Quick start (2 min)
  TESTING.md ────────────────→ Full testing guide
  STATUS.md ─────────────────→ Current status
  docs/REACT_GUIDE.md ───────→ Complete API docs
  examples/react-usage.tsx ──→ 12 examples


┌───────────────────────────────────────────────────────────────────────────┐
│ 🎯 ALL AVAILABLE HOOKS                                                    │
└───────────────────────────────────────────────────────────────────────────┘

  useSeo(config, deps?)
    → Basic SEO meta tags

  useStructuredData(schema, deps?)
    → JSON-LD structured data

  useBreadcrumbs(items, deps?)
    → Navigation breadcrumbs


┌───────────────────────────────────────────────────────────────────────────┐
│ 🎨 ALL AVAILABLE COMPONENTS                                               │
└───────────────────────────────────────────────────────────────────────────┘

  <SeoHead title="..." description="..." />
    → Component alternative to useSeo

  <JsonLd data={{...}} />
    → Component for structured data


┌───────────────────────────────────────────────────────────────────────────┐
│ ✅ SUCCESS CHECKLIST                                                      │
└───────────────────────────────────────────────────────────────────────────┘

  □ Browser tab title changes
  □ Live Inspector shows data
  □ Meta tags in DevTools
  □ No duplicate tags
  □ Tags update on navigation
  □ No console errors


┌───────────────────────────────────────────────────────────────────────────┐
│ 🐛 QUICK TROUBLESHOOTING                                                  │
└───────────────────────────────────────────────────────────────────────────┘

  Tags not updating?
    → Add dependencies: useSeo(config, [dep])

  Server won't start?
    → Try: python3 -m http.server 8080 --directory test-app

  React errors?
    → Install: npm install react react-dom

  More help?
    → See TESTING.md


┌───────────────────────────────────────────────────────────────────────────┐
│ 📊 WHAT TO EXPECT                                                         │
└───────────────────────────────────────────────────────────────────────────┘

  When working correctly:

  ✅ Page title changes instantly
  ✅ Meta tags update automatically
  ✅ Old tags removed (no duplicates)
  ✅ Structured data appears as JSON-LD
  ✅ Live Inspector shows all tags
  ✅ No console errors


┌───────────────────────────────────────────────────────────────────────────┐
│ 🌐 VALIDATE WITH SEO TOOLS                                                │
└───────────────────────────────────────────────────────────────────────────┘

  Google Rich Results Test:
    https://search.google.com/test/rich-results

  Facebook Sharing Debugger:
    https://developers.facebook.com/tools/debug/

  Twitter Card Validator:
    https://cards-dev.twitter.com/validator


┌───────────────────────────────────────────────────────────────────────────┐
│ 💡 PRO TIPS                                                               │
└───────────────────────────────────────────────────────────────────────────┘

  • Always add dependencies when using dynamic data
  • Keep descriptions under 160 characters
  • Use canonical URLs to avoid duplicate content
  • Test with real SEO tools before production
  • Add structured data for rich snippets


┌───────────────────────────────────────────────────────────────────────────┐
│ 📚 EXAMPLE: BLOG POST                                                     │
└───────────────────────────────────────────────────────────────────────────┘

  function BlogPost({ post }) {
    useSeo({
      title: `${post.title} - Blog`,
      description: post.excerpt,
      ogImage: post.image
    }, [post]);

    useStructuredData({
      '@context': 'https://schema.org',
      '@type': 'BlogPosting',
      headline: post.title,
      datePublished: post.date,
      author: { '@type': 'Person', name: post.author }
    }, [post]);

    return <article>...</article>;
  }


┌───────────────────────────────────────────────────────────────────────────┐
│ 🎯 NEXT STEPS                                                             │
└───────────────────────────────────────────────────────────────────────────┘

  1. ✅ Test app is open at http://localhost:3000
  2. ✅ Navigate through the 4 test pages
  3. ✅ Check Live SEO Inspector on each page
  4. ✅ Open DevTools and verify tags
  5. ✅ Use in your own React app!


╔═══════════════════════════════════════════════════════════════════════════╗
║  🎉 YOU'RE ALL SET! The test server is running. Start exploring!         ║
╚═══════════════════════════════════════════════════════════════════════════╝
