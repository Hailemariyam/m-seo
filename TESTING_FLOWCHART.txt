┌─────────────────────────────────────────────────────────────────────────────┐
│                     M-SEO REACT TESTING FLOWCHART                           │
└─────────────────────────────────────────────────────────────────────────────┘

                              START HERE
                                  │
                                  ▼
                    ┌─────────────────────────────┐
                    │  Run: npm run test:react    │
                    │  Server starts on :3000     │
                    └──────────────┬──────────────┘
                                   │
                                   ▼
                    ┌─────────────────────────────┐
                    │  Open Browser:              │
                    │  http://localhost:3000      │
                    └──────────────┬──────────────┘
                                   │
                                   ▼
        ┌──────────────────────────────────────────────────────┐
        │             TEST APP LOADS                           │
        │  ┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐   │
        │  │  Home  │  │  Blog  │  │Product │  │Breadcr.│   │
        │  └────────┘  └────────┘  └────────┘  └────────┘   │
        └──────────────────────────────────────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
                    ▼                             ▼
        ┌───────────────────────┐   ┌───────────────────────┐
        │  VISUAL VERIFICATION  │   │ TECHNICAL VERIFICATION│
        └───────────────────────┘   └───────────────────────┘
                    │                             │
                    ▼                             ▼
        ┌───────────────────────┐   ┌───────────────────────┐
        │  1. Browser tab title │   │  1. Open DevTools (F12)│
        │     changes           │   │                        │
        │                       │   │  2. Elements → <head>  │
        │  2. Live SEO Inspector│   │                        │
        │     shows tags        │   │  3. Find tags with:    │
        │                       │   │     data-mseo="true"   │
        │  3. Navigation smooth │   │     data-mseo-ld="true"│
        │                       │   │                        │
        │  4. No visual errors  │   │  4. Run console tests  │
        └───────────┬───────────┘   └───────────┬───────────┘
                    │                             │
                    └──────────────┬──────────────┘
                                   │
                                   ▼
                        ┌──────────────────┐
                        │   ALL WORKING?   │
                        └────┬────────┬────┘
                             │        │
                         YES │        │ NO
                             │        │
                    ┌────────▼──┐  ┌─▼─────────────────┐
                    │  SUCCESS! │  │  TROUBLESHOOT     │
                    │           │  │                    │
                    │  Use in   │  │ 1. Check console  │
                    │  your app │  │ 2. See TESTING.md │
                    └───────────┘  │ 3. Verify React   │
                                   │    is installed   │
                                   └───────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                        WHAT HAPPENS PER PAGE                                │
└─────────────────────────────────────────────────────────────────────────────┘

    User clicks → Home
         │
         ▼
    useSeo() runs
         │
         ├─→ document.title = "Home - M-SEO React Test App"
         │
         ├─→ Remove old <meta data-mseo> tags
         │
         ├─→ Add new <meta data-mseo> tags:
         │      • description
         │      • keywords
         │      • og:title, og:description, og:image
         │      • twitter:title, twitter:description, twitter:image
         │
         └─→ Add <link data-mseo> tags:
                • canonical
                • alternate

    User clicks → Blog
         │
         ▼
    useSeo() + useStructuredData() run
         │
         ├─→ All above (with blog data) +
         │
         └─→ Add <script data-mseo-ld type="application/ld+json">
                {
                  "@type": "BlogPosting",
                  "headline": "...",
                  "author": {...}
                }


┌─────────────────────────────────────────────────────────────────────────────┐
│                       VERIFICATION METHODS                                  │
└─────────────────────────────────────────────────────────────────────────────┘

Method 1: LIVE SEO INSPECTOR (Built into test app)
────────────────────────────────────────────────────
   ✅ Easiest method
   ✅ Real-time updates
   ✅ No technical knowledge needed

   Just scroll down on any page!


Method 2: BROWSER DEVTOOLS
────────────────────────────────────────────────────
   1. Press F12
   2. Go to Elements tab
   3. Expand <head>
   4. Look for:
      <meta data-mseo="true" ...>
      <link data-mseo="true" ...>
      <script data-mseo-ld="true" ...>


Method 3: CONSOLE COMMANDS
────────────────────────────────────────────────────
   Open console (F12 → Console) and run:

   // Show all meta tags
   console.table(
     Array.from(document.querySelectorAll('meta[data-mseo]'))
       .map(el => ({
         name: el.getAttribute('name') || el.getAttribute('property'),
         content: el.getAttribute('content')
       }))
   );

   // Show structured data
   document.querySelectorAll('script[data-mseo-ld]').forEach((el, i) => {
     console.log(`Schema ${i+1}:`, JSON.parse(el.textContent));
   });


┌─────────────────────────────────────────────────────────────────────────────┐
│                         FILE STRUCTURE                                      │
└─────────────────────────────────────────────────────────────────────────────┘

m-seo/
│
├── src/adapters/
│   └── ReactSPAAdapter.ts ──────→ Core implementation
│
├── examples/
│   └── react-usage.tsx ────────→ 12 complete examples
│
├── docs/
│   └── REACT_GUIDE.md ─────────→ Full documentation
│
├── test-app/
│   ├── index.html ─────────────→ Test page
│   ├── app.jsx ────────────────→ React test app
│   └── README.md ──────────────→ Quick start
│
├── TESTING.md ─────────────────→ Complete guide
├── TESTING_SUMMARY.md ─────────→ This summary
└── package.json ───────────────→ npm run test:react


┌─────────────────────────────────────────────────────────────────────────────┐
│                    QUICK REFERENCE COMMANDS                                 │
└─────────────────────────────────────────────────────────────────────────────┘

  npm run test:react ───→ Start test server (port 3000)
  npm run build ────────→ Build TypeScript to JavaScript
  npm run dev ──────────→ Watch mode (auto-rebuild)
  npm test ─────────────→ Run vanilla JS tests


┌─────────────────────────────────────────────────────────────────────────────┐
│                         SUCCESS CHECKLIST                                   │
└─────────────────────────────────────────────────────────────────────────────┘

  □ Server running on http://localhost:3000
  □ Page loads without errors
  □ Can navigate between pages
  □ Browser tab title changes
  □ Live SEO Inspector shows data
  □ DevTools shows <meta data-mseo> tags
  □ DevTools shows <script data-mseo-ld> tags
  □ No duplicate tags
  □ Tags update when navigating
  □ No console errors


┌─────────────────────────────────────────────────────────────────────────────┐
│                    READY TO USE IN YOUR APP?                                │
└─────────────────────────────────────────────────────────────────────────────┘

  import { useSeo, useStructuredData } from 'm-seo/adapters/ReactSPAAdapter';

  function MyPage() {
    useSeo({
      title: 'My Page',
      description: 'Description here',
      keywords: ['react', 'seo']
    });

    return <div>Content</div>;
  }


═══════════════════════════════════════════════════════════════════════════════
                              YOU'RE ALL SET! 🎉
═══════════════════════════════════════════════════════════════════════════════
