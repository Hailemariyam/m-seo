# VitePress Docs

This folder contains the configuration for your documentation website. To run locally:

```bash
npm install vitepress
npx vitepress dev docs-site
```

To build static site:

```bash
npx vitepress build docs-site
```
